import * as DCL from "decentraland-api";
export declare type Frame = {
    width: number;
    height: number;
    x: number;
    y: number;
};
export declare type Frames = {
    [key: string]: Frame;
};
export declare function createAtlas<T extends Frames, K extends keyof T>(opts: {
    material: string;
    textureSize: {
        width: number;
        height: number;
    };
    frames: T;
}): (props: JSX.BaseEntity & {
    color?: string | number;
    material?: string;
    withCollisions?: boolean;
} & {
    frame: K;
}) => DCL.ISimplifiedNode;
