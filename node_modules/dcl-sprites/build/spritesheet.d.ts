import * as DCL from "decentraland-api";
export declare function createSpriteSheet(opts: {
    material: string;
    textureSize: {
        width: number;
        height: number;
    };
    cellSize: {
        width: number;
        height: number;
    };
}): (props: JSX.BaseEntity & {
    color?: string | number;
    material?: string;
    withCollisions?: boolean;
} & {
    frame: string;
}) => DCL.ISimplifiedNode;
